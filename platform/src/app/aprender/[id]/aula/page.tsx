import { Classroom } from "@/components/classroom";
export default async function Page({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ aula?: string; t?: string; previa?: string }> }) { const { id } = await params; const query = await searchParams; const position=query.t === undefined ? undefined : Number(query.t); return <Classroom id={id} initialLesson={query.aula} initialPosition={position !== undefined && Number.isFinite(position) && position >= 0 ? position : undefined} preview={query.previa === "1"}/>; }
