import { Classroom } from "@/components/classroom";
export default async function Page({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ aula?: string; t?: string; previa?: string }> }) { const { id } = await params; const query = await searchParams; const position=Number(query.t); return <Classroom id={id} initialLesson={query.aula} initialPosition={Number.isFinite(position)&&position>0?Math.floor(position):0} preview={query.previa === "1"}/>; }
