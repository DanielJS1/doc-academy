{
  "name": "doc-academy",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --dir platform dev",
    "build": "pnpm --dir platform build",
    "start": "pnpm --dir platform start",
    "typecheck": "pnpm --dir platform typecheck",
    "test": "pnpm --dir platform test",
    "backup:network": "powershell -ExecutionPolicy Bypass -File ./scripts/sync-backup-to-network.ps1"
  }
}
